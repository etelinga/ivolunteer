#
# Put messages in here that are defined using annotations on the FormModel class and have specfic message references.
# For example,  @Length(min=1, max=50, message="{comment.short}" )
# For messages where an error is placed directly in the controller, those messages are in ApplicationResources,
# e.g. error.rejectValue("field", "message")
#
field.required=Please enter

#Stuff validator
# description.short=Really? Please give your stuff a name with a few characters.  Keeping as an example.

#Comment validator
comment.short=Please say something. At least a few characters.

distance.range=Please enter a distance from you zip code between the given range.

# Default validator messages from Spring jar
validator.assertFalse=assertion failed
validator.assertTrue=assertion failed
validator.future=must be a future date
validator.length=length must be between {min} and {max}
validator.max=must be less than or equal to {value}
validator.min=must be greater than or equal to {value}
validator.notNull=may not be null
validator.past=must be a past date
validator.pattern=must match "{regex}"
validator.range=must be between {min} and {max}
validator.size=size must be between {min} and {max}
validator.email=Please enter an e-mail address
validator.notEmpty=may not be null or empty
validator.digits=Numeric value out of bounds (<{integerDigits} digits>.<{fractionalDigits} digits> expected)
validator.creditCard=Invalid credit card number
validator.ean=Invalid EAN